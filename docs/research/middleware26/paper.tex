\documentclass[sigconf,9pt,anonymous]{acmart}

\usepackage{booktabs} % For formal tables
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{microtype}
\usepackage{array}
\usepackage{multirow}
\usepackage{tabularx}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows, positioning, fit, backgrounds}

% Disable ACM copyright/permission block for submission
\settopmatter{printacmref=true}
\setcopyright{none}
\acmConference[Middleware '26]{ACM International Middleware Conference}{December 7--11, 2026}{Pisa, Italy}
\acmYear{2026}
\acmDOI{}
\acmISBN{}

% Compact padding
\setlength{\tabcolsep}{3.5pt}

% Compact bibliography spacing
\let\oldthebibliography\thebibliography
\let\endoldthebibliography\endthebibliography
\renewenvironment{thebibliography}[1]{%
  \begin{oldthebibliography}{#1}%
    \setlength{\itemsep}{0pt}%
    \setlength{\parskip}{0pt}%
}{%
  \end{oldthebibliography}%
}

\begin{document}

\title[Architectural Digital Twin for ATM Middleware]{An Architectural Digital Twin for Pre-Deployment Verification and CI/CD Gating in Air Traffic Management Middleware Systems (Industry Track)}

\author{Aleksander Slominski}
\affiliation{%
  \institution{IBM Research}
  \city{Yorktown Heights}
  \state{NY}
  \country{USA}
}
\email{aslom@us.ibm.com}

\author{Javier Picorel}
\affiliation{%
  \institution{Huawei Research}
  \city{Munich}
  \country{Germany}
}
\email{javier.picorel@huawei.com}

\author{Eduard Marin}
\affiliation{%
  \institution{Telefonica}
  \city{Madrid}
  \country{Spain}
}
\email{eduard.marin@telefonica.com}

\author{Onur Alp Yigit}
\affiliation{%
  \institution{System as a Graph Research Team}
  \city{Ankara}
  \country{Turkey}
}
\email{onuralp@systemasagraph.org}

\begin{abstract}
Modern mission-critical Air Traffic Management (ATM) systems rely on complex distributed middleware infrastructures deployed across multi-tier controller workstations, surveillance radar processing gateways, and flight data processing systems. In continuous delivery pipelines, software updates undergo frequent releases across varying operational scales---from terminal maneuvering areas (TMA) to multi-center en-route sectors. However, non-local architectural misconfigurations, such as Quality-of-Service (QoS) parameter incompatibilities, conflicting hardware processor core allocations, memory parameter misalignments, and circular package dependencies, frequently pass unit and integration testing. These defects manifest only as costly post-deployment failures or runtime degradations in target operational airspace environments.

This paper presents \textbf{System as a Graph (SaaG)}, an architectural digital twin for pre-deployment verification and continuous deployment gating in ATM middleware systems. The twin is \emph{static}: a typed multigraph ($G = (V, E)$) reconstructed fresh from candidate software versions, avoiding the complexity of live-synchronized runtime models. We contribute three main results: (1) an industrial scale ATM digital twin graph schema and requirements baseline specifying 5 entity classes, 6 structural relations, and 6 logical dependency rules derived from physical pub/sub linkage; (2) a CI/CD pipeline gate combining an implemented severity-based exit-code gate with a specified 4-heading installation suitability scoring model; and (3) performance benchmarks across 5 operational profiles (29 to 444 components) demonstrating sub-second verification latencies (0.01\,s to 1.01\,s). Furthermore, we document an industrial gap analysis showing that 4 of 7 specified verification checks are blocked on configuration data acquisition rather than graph analysis techniques, and detail a structured experimental methodology to evaluate defect detection effectiveness, detector precision scaling, and GNN-based criticality ranking upon completion of full SaaG-D system deployment.
\end{abstract}

\ccsdesc[500]{Computer systems organization~Real-time systems}
\ccsdesc[500]{Software and its engineering~Software maintenance tools}

\keywords{Air Traffic Management (ATM), Architectural Digital Twin, Middleware Verification, Pub/Sub QoS Contracts, CI/CD Gating, CPU Core Pinning, Static Rule Auditing}

\maketitle

\section{Introduction \& Industrial ATM Problem Statement}

Continuous Integration and Continuous Delivery (CI/CD) pipelines have streamlined building and testing software artifacts. However, in mission-critical Air Traffic Management (ATM) environments governed by publish/subscribe (pub/sub) middleware, verifying an assembled distributed system prior to installation remains a major operational challenge.

\subsection{The Pre-Deployment Verification Gap in ATM Systems}

Governed by the ICAO Global ATM Operational Concept~\cite{icao2005}, ATM systems execute across multi-core processing servers, radar processing gateways, and controller workstations. Software units exchange high-frequency telemetry and flight control data via pub/sub channels.

When a candidate software build is proposed for release into an operational sector, conventional CI/CD pipelines evaluate unit test suites in isolation. Consequently, systemic architectural misconfigurations pass unnoticed:

\begin{itemize}\itemsep=0.5pt
    \item \textbf{Hardware Core Contention:} Latency-sensitive surveillance processes pinned to overlapping CPU cores, or host node allocations exceeding physical CPU core capacities on multi-core servers.
    \item \textbf{Middleware QoS Contract Incompatibilities:} Mismatched Quality-of-Service (QoS) parameters on critical safety channels. For instance, a subscriber expecting \texttt{TRANSIENT\_LOCAL} durability for emergency clearance commands bound to a publisher configured for \texttt{VOLATILE} delivery; in pub/sub middleware, mismatched QoS contracts cause silent transport disconnections.
    \item \textbf{Silent Topic Disconnections:} Software units publishing to or consuming from topics with schema definitions that diverge across releases, or refactored topics left with no active subscribers.
    \item \textbf{Circular Package Dependencies:} Transitive dependency cycles between flight plan processors and 4D trajectory predictors that manifest as initialization deadlocks during sector start-up.
\end{itemize}

Building testbeds for every candidate build across facilities is prohibitively slow and expensive. Furthermore, staging networks fail to capture full operational loads, leaving architectural configuration defects to be discovered during live operations.

\subsection{The SaaG Approach \& Contributions}

To bridge this gap, we present \textbf{System as a Graph (SaaG)}, an architectural digital twin framework for pre-deployment verification in ATM middleware systems. SaaG constructs a static, multi-layered digital model of the candidate system topology without executing application binaries. Operating directly within CI/CD workflows, SaaG statically audits hardware core bindings, topic QoS parameters, and package dependencies before software installation.

This paper makes the following contributions:
\begin{enumerate}\itemsep=0.5pt
    \item \textbf{ATM Digital Twin Schema \& Requirements Baseline (\S2):} A formal graph representation ($G = (V, E, \tau_V, \tau_E, w)$) mapping 5 entity classes and 6 relation types, supported by a verifiable requirements baseline developed with system architects.
    \item \textbf{CI/CD Gating \& Industrial Gap Analysis (\S3, \S4):} A two-stage deployment gate comprising an implemented severity exit-code gate (codes 0/1/2) and a specified 4-heading Installation Suitability Scoring model ($\mathcal{S}$). We report an explicit gap analysis showing why 4 of 7 specified verification checks are blocked on input data acquisition.
    \item \textbf{Multi-Scale Performance \& Evaluation Plan (\S5):} Empirical benchmarks across 5 scaling ATM deployment profiles (29 to 444 components) demonstrating sub-second audit latencies (0.01\,s to 1.01\,s), accompanied by a structured experimental methodology to evaluate defect detection, detector precision scaling, and criticality model accuracy upon completion of full system deployment.
\end{enumerate}

\section{System Overview \& ATM Digital Twin Graph Model}

\begin{figure}[t]
\centering
\begin{tikzpicture}[node distance=0.75cm, font=\sf\scriptsize, >=stealth]
  \tikzstyle{block} = [draw, fill=blue!5, rectangle, rounded corners, minimum width=7.0cm, minimum height=0.46cm, align=center]
  \tikzstyle{subblock} = [draw, fill=green!5, rectangle, rounded corners, minimum width=3.3cm, minimum height=0.50cm, align=center]
  
  \node (sources) [block] {\textbf{ATM Data Sources}\\ \textit{[CMDB Metadata] \; [Source Repos] \; [Package Registry] \; [Network Descriptor]}};
  \node (msd) [block, below=of sources] {\textbf{Model Setup Data Generation (SaaG-MSD)}\\ \textit{Ingestion, Metadata Tagging (Req 1.5--1.18) \& Schema Validation}};
  \node (csm) [block, below=of msd] {\textbf{Node-Relationship Core System Model (SaaG-CSM)}\\ \textit{Candidate Isolation (Req 5.1--5.5) \& Multigraph $G = (V, E, \tau_V, \tau_E, w)$}};
  
  \node (vae) [subblock, below left=0.6cm and -0.2cm of csm] {\textbf{Verification Engine (VAE)}\\ \textit{QoS Rules \& Core Pinning}};
  \node (frd) [subblock, below right=0.6cm and -0.2cm of csm] {\textbf{Telemetry Overlay (FRD/ADP)}\\ \textit{Architectural Drift Detection}};
  
  \node (gate) [block, below=1.35cm of csm] {\textbf{ATM CI/CD Pipeline Gating (Jenkins / CLI)}\\ \textit{[Exit 0/1: Pass $\rightarrow$ Deploy Sector] \quad [Exit 2: Fail $\rightarrow$ Abort \& Report]}};
  
  \draw[->, thick] (sources) -- (msd);
  \draw[->, thick] (msd) -- (csm);
  \draw[->, thick] (csm) -| (vae);
  \draw[->, thick] (csm) -| (frd);
  \draw[->, thick] (vae) |- (gate);
  \draw[->, thick] (frd) |- (gate);
\end{tikzpicture}
\caption{SaaG Architectural Digital Twin Pipeline Integration Overview for ATM Systems.}
\label{fig:architecture}
\end{figure}

\subsection{Static Scoping \& System Capabilities}

``Digital twin'' is scoped precisely: SaaG builds a \textbf{static twin}---a typed graph reconstructed fresh for each candidate build, rather than a continuously live-synchronized model. A complete digital twin possesses a dynamic half (live telemetry overlay and design-versus-observed drift detection). In SaaG, this dynamic half is specified in the requirements baseline, but separated from the static verification engine reported here.

To enforce clarity, we distinguish \textbf{SaaG-D} (will be deployed baseline system specification) from \textbf{SaaG-P} (the open verification prototype).

\subsection{Model Setup Data Generation (SaaG-MSD)}

The \textbf{Model Setup Data Generation (SaaG-MSD)} component interfaces with four enterprise repositories: CMDB metadata, source control repositories, package registries, and network topology descriptors. Each acquired record is tagged with project, platform, sector ID, and candidate release version numbers.

\subsection{Core System Model Formal Graph Definition (SaaG-CSM)}

The \textbf{Core System Model (SaaG-CSM)} transforms ingested descriptors into a multi-attributed, weighted directed multigraph:
\begin{equation}
G = (V, E, \tau_V, \tau_E, w_V, w_E)
\end{equation}

\subsubsection{ATM Vertex Entity Types ($V$)}
The vertex set $V = V_{\text{app}} \cup V_{\text{broker}} \cup V_{\text{topic}} \cup V_{\text{node}} \cup V_{\text{lib}}$ classifies 5 entity types:
\begin{itemize}\itemsep=0.5pt
    \item \textbf{Applications ($V_{\text{app}}$):} Executable ATM software units.
    \item \textbf{Brokers ($V_{\text{broker}}$):} ATM message brokers.
    \item \textbf{Topics ($V_{\text{topic}}$):} Pub/sub channels.
    \item \textbf{Infrastructure Nodes ($V_{\text{node}}$):} Processing hardware hosts.
    \item \textbf{Libraries ($V_{\text{lib}}$):} Shared code modules.
\end{itemize}

\subsubsection{Structural Edge Types ($E_{\text{structural}}$)}
Six structural edge types are imported: \texttt{PUBLISHES\_TO} ($(V_{\text{app}} \cup V_{\text{lib}}) \times V_{\text{topic}}$), \texttt{SUBSCRIBES\_TO} ($V_{\text{topic}} \times (V_{\text{app}} \cup V_{\text{lib}})$), \texttt{ROUTES} ($V_{\text{broker}} \times V_{\text{topic}}$), \texttt{RUNS\_ON} ($(V_{\text{app}} \cup V_{\text{broker}}) \times V_{\text{node}}$), \texttt{CONNECTS\_TO} ($V_{\text{node}} \times V_{\text{node}}$), and \texttt{USES} ($(V_{\text{app}} \cup V_{\text{lib}}) \times V_{\text{lib}}$).

\subsubsection{Derived Dependency Projection Rules ($E_{\text{dependency}}$)}
Physical pub/sub data flows from publisher to subscriber. However, structural failure dependencies point in the opposite direction: a subscriber depends on the publisher supplying its topic data. SaaG derives \texttt{DEPENDS\_ON} edges pointing \textbf{from dependent to dependency}:

\begin{table}[h]
\centering
\caption{Derived Logical Dependency Edge Projection Rules.}
\label{tab:dep_rules}
\small
\begin{tabular}{c l l c}
\toprule
\textbf{Rule} & \textbf{Type} & \textbf{Derivation Pattern} & \textbf{Weight $w(e)$} \\
\midrule
1 & \texttt{app\_to\_app} & App/Lib \texttt{SUB} $t \leftarrow$ \texttt{PUB} App/Lib & $\max_{t} w(t)$ \\
2 & \texttt{app\_to\_broker} & App/Lib \texttt{PUB}/\texttt{SUB} $t \leftarrow$ \texttt{ROUTES} Broker & $\max_{t} w(t)$ \\
3 & \texttt{node\_to\_node} & Lifted from \texttt{app\_to\_app} between hosted apps & Lifted $\max(w)$ \\
4 & \texttt{node\_to\_broker} & Lifted from \texttt{app\_to\_broker} for hosted apps & Lifted $\max(w)$ \\
5 & \texttt{app\_to\_lib} & App/Lib \texttt{USES} $\rightarrow$ Library & Inherits $w(\text{app})$ \\
6 & \texttt{broker\_to\_broker} & Colocation edge sharing physical Node & Inherits $w(\text{node})$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Intrinsic Pub/Sub QoS \& Upward Weight Propagation}

SaaG assigns criticality weights $w(v) \in [0, 1]$ across all entities using an Analytical Hierarchy Process (AHP) QoS formula~\cite{saaty1980}:

\begin{enumerate}
    \item \textbf{Intrinsic Topic Weight Formula:} For each topic $t \in V_{\text{topic}}$,
    \begin{equation}
    w(t) = \max\left(0.01, \; \beta \cdot \text{QoS\_score}(t) + (1 - \beta) \cdot \text{size\_norm}(t)\right)
    \end{equation}
    where $\beta = 0.85$, $\text{size\_norm}(t) = \min\left(\frac{\log_2(1 + \text{size\_kb})}{50}, \, 1.0\right)$, and:
    \begin{equation}
    \text{QoS\_score}(t) = 0.30 \cdot \text{rel} + 0.40 \cdot \text{dur} + 0.30 \cdot \text{prio}
    \end{equation}
    Reliability (\texttt{RELIABLE}=1.0, \texttt{BEST\_EFFORT}=0.0), Durability (\texttt{PERSISTENT}=1.0, \texttt{TRANSIENT\_LOCAL}=0.5, \texttt{VOLATILE}=0.0), and Transport Priority (\texttt{CRITICAL}=1.0, \texttt{HIGH}=0.66, \texttt{LOW}=0.0).

    \item \textbf{Upward Weight Propagation:} Component weights propagate hierarchically:
    \begin{align}
    w(a) &= 0.80 \cdot \max_{t \in T(a)} w(t) + 0.20 \cdot \text{mean}_{t \in T(a)} w(t) \\
    w(b) &= 0.70 \cdot \max_{t \in T(b)} w(t) + 0.30 \cdot \text{mean}_{t \in T(b)} w(t) \\
    w(l) &= \min\left(1.0, \; \text{base\_w} \cdot (1 + \gamma \cdot \log_2(1 + DG_{\text{in}}(l)))\right) \\
    w(n) &= \max_{v \text{ RUNS\_ON } n} w(v)
    \end{align}
    where $\gamma = 0.15$ accounts for library fan-out amplification.
\end{enumerate}

\subsection{Multi-Session Process-Isolated Candidate Modeling}

During concurrent builds, SaaG constructs an isolated candidate graph $G_{u'} = (V', E')$ by substituting candidate unit version $u'$ into the target baseline inventory under a unique process identifier.

\section{Verification Engine \& Industrial Gap Analysis}

The \textbf{Verification and Analysis (SaaG-VAE)} engine executes rule-based static audits, cascade simulations, and telemetry overlays on candidate digital twins $G_{u'}$.

\subsection{Implemented Rule Audits}

\begin{enumerate}\itemsep=0.5pt
    \item \textbf{Middleware Topic QoS Conformance:} For every topic $v_t \in V_{\text{topic}}$, publishers $P(v_t)$ and subscribers $S(v_t)$ are audited for QoS policy matching. If subscriber durability or reliability exceeds publisher guarantees, a critical incompatibility finding is logged.
    \item \textbf{Processor Core Pinning \& Contention:} For each host node $v_p \in V_{\text{node}}$ with physical core capacity $C(v_p)$, SaaG verifies $\sum_{u \in U(v_p)} |\text{Cores}(u)| \le C(v_p)$ and enforces non-overlapping core sets ($\text{Cores}(u_i) \cap \text{Cores}(u_j) = \emptyset$) for latency-sensitive processes.
    \item \textbf{Orphaned Topics \& Schema Consistency:} Flags un-subscribed topics ($|S(v_t)| = 0$), un-published topics ($|P(v_t)| = 0$), and mismatched ASTERIX message category payloads across endpoints.
    \item \textbf{Circular Dependencies:} Tarjan's Strongly Connected Components algorithm~\cite{tarjan1972} detects circular package references.
\end{enumerate}

\subsection{Field Telemetry Overlay \& Synthetic Cascade Simulation}

\begin{itemize}\itemsep=0.5pt
    \item \textbf{Telemetry Overlay:} Operational telemetry from the Field Records Database is bound to graph nodes. SaaG detects architectural drift via $\text{Drift}_{\text{Undeclared}} = E_{\text{Observed}} \setminus E_{\text{Designed}}$.
    \item \textbf{Cascade Impact Simulation:} The Scenario Generator simulates node failures or message bursts along structural edges ($E_{\text{structural}}$), establishing a blast-radius reference oracle for scoring detector accuracy.
\end{itemize}

\subsection{Specified vs. Implemented Gap Analysis}

A key finding for industrial practitioners is the gap between specified verification requirements and actual prototype implementation. Table~\ref{tab:gap_analysis} enumerates 7 specified capabilities in SaaG-D that were not fully realized in SaaG-P:

\begin{table*}[t]
\centering
\caption{Specified vs. Implemented Gap Analysis in SaaG Verification Capability.}
\label{tab:gap_analysis}
\small
\begin{tabularx}{\textwidth}{l X X}
\toprule
\textbf{Verification Capability} & \textbf{Status in Prototype (SaaG-P)} & \textbf{Primary Operational Blocker} \\
\midrule
Endpoint QoS Conformance & Topic-level QoS anomaly check & \textbf{Data Ingestion:} Endpoint-level IDL descriptors missing \\
Payload Schema Matching & Topic string identity match & \textbf{Data Ingestion:} Message IDL field parser not integrated \\
Hardware Core Allocation & Core capacity attribute flag & \textbf{Data Ingestion:} OS core assignment files live outside build repos \\
OS Memory \& Kernel Audit & Config descriptor requirement & \textbf{Data Ingestion:} Target OS parameters not exported to CMDB \\
Live Architectural Drift & Static vs observed graph diff & \textbf{Data Ingestion:} Operational field log telemetry store unbuilt \\
Scored Suitability Model & Severity exit-code gate (0/1/2) & \textbf{Implementation:} Scoring weights require empirical tuning \\
CMDB \& Network Ingestion & JSON system descriptor files & \textbf{Data Ingestion:} Enterprise CMDB REST API integration pending \\
\bottomrule
\end{tabularx}
\end{table*}

\vspace{1pt}
\noindent\textbf{Industrial Insight:} \emph{4 of the 7 unbuilt capabilities are blocked on \textbf{input data acquisition} (extracting configuration files from enterprise repositories into the graph pipeline), not on graph analysis algorithms.}

\section{CI/CD Pipeline Integration \& Automated Gating}

\begin{figure}[h]
\centering
\begin{tikzpicture}[node distance=0.9cm, font=\sf\scriptsize, >=stealth]
  \node (dev) [draw, fill=gray!10, minimum width=0.95cm] {Developer};
  \node (git) [draw, fill=gray!10, right=0.5cm of dev, minimum width=0.95cm] {Git Repo};
  \node (ci) [draw, fill=gray!10, right=0.5cm of git, minimum width=0.95cm] {Jenkins CI};
  \node (saag) [draw, fill=blue!10, right=0.5cm of ci, minimum width=0.95cm] {SaaG Engine};
  \node (env) [draw, fill=gray!10, right=0.5cm of saag, minimum width=0.95cm] {Target ATM};
  
  \draw[->] (dev) -- node[above] {push} (git);
  \draw[->] (git) -- node[above] {webhook} (ci);
  \draw[->] (ci) -- node[above] {invoke gate} (saag);
  \draw[->] (saag) to [out=60,in=120,looseness=3] node[above] {audit $G_{u'}$} (saag);
  \draw[->] (saag) -- node[above] {exit code} (ci);
  \draw[->, dashed] (ci) -- node[above] {deploy (exit 0/1)} (env);
  \draw[->, dotted] (ci) |- node[below] {abort build (exit 2)} (dev);
\end{tikzpicture}
\caption{Sequence Diagram of SaaG Automated CI/CD Deployment Gating.}
\label{fig:sequence}
\end{figure}

\subsection{Implemented Severity Exit-Code Gate \& Limitations}

SaaG-P integrates into continuous delivery pipelines via a command-line interface. It evaluates candidate graph $G_{u'}$, runs detectors, and encodes its decision into standard process exit status:
\begin{equation}
\text{Exit Status} = \begin{cases} 2, & \text{if any CRITICAL or HIGH finding is detected} \\ 1, & \text{if MEDIUM or LOW findings are detected} \\ 0, & \text{if zero findings are detected} \end{cases}
\end{equation}

Standard CI runners (e.g., Jenkins, GitHub Actions) treat exit code 2 as a build failure, aborting deployment without extra plugins.

\subsubsection{Absolute vs. Delta-Aware Gating Limitation}
The implemented gate evaluates the candidate system on an absolute basis rather than measuring deltas against the merge base. If a codebase carries pre-existing HIGH findings, every subsequent candidate build returns exit code 2. An absolute gate deployed without a waiver register would block all pipeline releases. In production pipelines, gating must be \emph{delta-aware}---blocking candidate builds only when new CRITICAL or HIGH findings are introduced.

\subsection{Specified Scored Installation Suitability Model}

SaaG-D is specified to replace binary exit status with a quantitative \textbf{Installation Suitability Score} ($\mathcal{S}$) evaluated across four headings: (1) Structural \& Architectural Conformance ($H_1$), (2) Interface \& Topic Conformance ($H_2$), (3) Dependency \& Integration Conformance ($H_3$), and (4) Resource \& Performance Sufficiency ($H_4$).

\begin{equation}
\mathcal{S} = 100 - \sum_{k \in \text{Violations}} W(r_k) \cdot \text{Penalty}(S(r_k))
\end{equation}

\begin{equation}
\text{Decision}(u') = \begin{cases} \text{FAIL}, & \text{if } \exists k \text{ s.t. } S(r_k) = \text{Critical} \lor B(r_k) = 1 \\ \text{FAIL}, & \text{if } \mathcal{S} < \mathcal{S}_{\text{threshold}} \\ \text{PASS}, & \text{otherwise} \end{cases}
\end{equation}

\subsection{Actionable Findings \& Evidence-Bearing Reports}

SaaG outputs structured JSON finding reports containing the rule identifier, affected component, severity, root-cause description, and exact supporting evidence (e.g., conflicting core IDs or mismatched QoS enums), providing developers with diagnostic guidance.

\section{Industrial ATM Evaluation \& Multi-Scale Performance}

\subsection{Experimental Setup Across 5 ATM Scale Profiles}

We evaluated SaaG performance across 5 scaling operational profiles modeled under the ICAO Global ATM Operational Concept framework:
\begin{enumerate}\itemsep=0.5pt
    \item \textbf{Scale Tiny (Approach Sector):} 10 Apps, 8 Topics, 2 Brokers, 6 Nodes, 3 Libraries (29 Components).
    \item \textbf{Scale S (TMA Sector):} 26 Apps, 27 Topics, 5 Brokers, 8 Nodes, 8 Libraries (74 Components).
    \item \textbf{Scale M (Regional ACC Center):} 52 Apps, 54 Topics, 10 Brokers, 16 Nodes, 16 Libraries (148 Components).
    \item \textbf{Scale L (Multi-Sector ACC Cluster):} 104 Apps, 108 Topics, 20 Brokers, 32 Nodes, 32 Libraries (296 Components).
    \item \textbf{Scale XL (National ATM Grid):} 156 Apps, 162 Topics, 30 Brokers, 48 Nodes, 48 Libraries (444 Components).
\end{enumerate}

Across scales, QoS mix, fan-out shape, and criticality proportions were held constant, isolating system scale as the sole independent variable. Each scale point was executed across 5 random seeds ($\{42, 123, 456, 789, 2024\}$).

\subsection{Verification Cost \& CI/CD Overhead}

We evaluated end-to-end gate invocation latency across 500 candidate build evaluations per scale:

\begin{table}[h]
\centering
\caption{Gate Execution Latency Across 5 ATM Scale Profiles.}
\label{tab:latency}
\small
\begin{tabular}{l c c c c}
\toprule
\textbf{Scale Profile} & \textbf{Components} & \textbf{Graph Const. (s)} & \textbf{Rule Audit (s)} & \textbf{Total Gate (s)} \\
\midrule
Scale Tiny & 29 & 0.008 $\pm$ 0.001 & 0.002 $\pm$ 0.000 & \textbf{0.010 $\pm$ 0.001} \\
Scale S & 74 & 0.042 $\pm$ 0.002 & 0.008 $\pm$ 0.001 & \textbf{0.050 $\pm$ 0.003} \\
Scale M & 148 & 0.112 $\pm$ 0.005 & 0.018 $\pm$ 0.002 & \textbf{0.130 $\pm$ 0.006} \\
Scale L & 296 & 0.485 $\pm$ 0.018 & 0.045 $\pm$ 0.004 & \textbf{0.530 $\pm$ 0.022} \\
Scale XL & 444 & 0.925 $\pm$ 0.031 & 0.085 $\pm$ 0.007 & \textbf{1.010 $\pm$ 0.035} \\
\bottomrule
\end{tabular}
\end{table}

\vspace{1pt}
\noindent\textbf{Performance Result:} \emph{Verification latency scales gently with system size. On the \textbf{Scale XL National Grid} (444 components, 1,200+ edges), mean audit latency is \textbf{1.01 seconds} (P95 < 1.5\,s), demonstrating negligible pipeline overhead.}

\subsection{Planned Evaluation: Defect Detection Effectiveness in Industrial Pipelines}

To measure the empirical effectiveness of SaaG pre-deployment gating once full SaaG-D development concludes, we have designed a 6-month trial protocol across candidate ATM staging pipelines. In this planned trial, all candidate software builds will be passed through the static verification engine prior to staging deployment.

Caught pre-deployment architectural defects will be tracked and categorized across five primary failure classes:
(1) Hardware Core Over-Allocation \& Conflict on latency-sensitive nodes (\texttt{surveillance-server}, \texttt{fdps-main});
(2) Middleware Topic QoS Parameter Mismatches on safety channels (\texttt{clearance.cmd}, \texttt{radar.tracks});
(3) Orphaned Topics \& Schema Discord;
(4) Circular Software Package Dependencies; and
(5) Memory \& OS Configuration Incongruity exceeding physical target host bounds.

\subsection{Planned Evaluation: Detector Catalog vs. Cascade Propagation Oracle}

To evaluate detector precision and scaling behavior on dense architectural graphs, we plan to score the static rule detector catalog against a simulated cascade blast-radius oracle across the 5 ATM scale profiles (Scale Tiny through Scale XL).

For each scale profile, candidate graph instances will be subjected to synthetic failure injection and message burst propagation along structural edges ($E_{\text{structural}}$) to establish ground-truth cascade impact zones. The static rule detectors will be scored using standard binary classification metrics: Precision, Recall, $F_1$ score, and inter-rater agreement via Cohen's $\kappa$ coefficient. This study will specifically test the hypothesis of whether static rule-based auditing suffers from structural over-flagging (false positives) as graph density scales up, and measure the degree of precision degradation across system sizes.

\subsection{Planned Evaluation: Criticality Ranking Model Comparison}

To assess component criticality scoring $Q^*(v)$ on the reference ATM dataset (Scale S), we have structured a comparative benchmark comparing 6 graph representation learning formulations against simulation ground-truth oracles:
\begin{enumerate}\itemsep=0.5pt
    \item \texttt{Topo-BL}: Unweighted Topological Centrality (betweenness and degree).
    \item \texttt{Topo-QoS}: Local QoS-Weighted Centrality.
    \item \texttt{GL}: Homogeneous Graph Attention Network (GAT).
    \item \texttt{GL-QoS}: QoS-Weighted Homogeneous GAT.
    \item \texttt{HGL}: Heterogeneous GAT (QoS Masked).
    \item \textbf{\texttt{HGL-QoS}}: QoS-Aware Heterogeneous GAT (Proposed SaaG Model).
\end{enumerate}

The ranking models will be evaluated using Spearman's rank correlation ($\rho$), Top-$K$ $F_1$ score ($K=20$), Normalized Discounted Cumulative Gain (NDCG@10), and Single Point of Failure (SPOF) identification $F_1$ score upon completion of full SaaG-D training runs.

\subsection{Operational Impact \& Threats to Validity}

\begin{itemize}\itemsep=0.5pt
    \item \textbf{Anticipated Operational Impact:} Upon full deployment in target staging pipelines, SaaG gating is designed to reduce post-deployment middleware-related operational incidents and accelerate configuration diagnostic times from hours to seconds by producing evidence-bearing JSON reports.
    \item \textbf{Threats to Validity:} (1) Benchmark detection accuracy will be scored against a cascade simulator, which may not capture all unexpected field hardware dynamics; (2) The generator scales ATM topologies by replicating core application roles, which may differ from real-world sector expansion; (3) SaaG-P implements a subset of SaaG-D's specified capabilities as documented in the gap analysis (\S3.3).
\end{itemize}

\section{Related Work}

\begin{itemize}\itemsep=0.5pt
    \item \textbf{Architecture Description Languages (ADLs):} Languages like AADL and SysML support formal analysis of hardware/software interactions~\cite{feiler2012aadl}. However, manual model maintenance causes ADLs to drift from implementation code in continuous delivery pipelines. SaaG automates graph model construction per candidate build directly from build artifacts.
    \item \textbf{Static Code Analysis Tools:} Analyzers like SonarQube or Coverity evaluate syntax and local control flows within individual repositories. They cannot observe multi-node topologies, CPU core pinning, or pub/sub QoS contracts across system boundaries. SaaG operates at the architectural system layer.
    \item \textbf{Runtime Application Performance Monitoring (APM):} Observability frameworks (Prometheus, Dynatrace) track distributed systems post-deployment. While accurate, APMs operate reactively after non-conforming software is deployed. SaaG operates proactively in CI/CD pipelines.
    \item \textbf{Digital Twins in Manufacturing vs. Software:} In Industry 4.0, digital twins synchronize physical assets via live sensor feeds~\cite{tao2019digital}. SaaG adapts the digital twin concept to \emph{software architecture}, scoping its primary verification engine as a static model reconstructed per candidate build.
\end{itemize}

\section{Conclusion \& Future Work}

We presented \textbf{SaaG (System as a Graph)}, an architectural digital twin for pre-deployment verification and continuous deployment gating in mission-critical Air Traffic Management middleware systems. By constructing a multi-attributed multigraph ($G = (V, E)$) from candidate build artifacts, SaaG statically audits hardware core allocations, pub/sub QoS policies (\texttt{durability}, \texttt{reliability}, \texttt{transport\_priority}), circular dependencies, and topic integrity prior to installation. Performance benchmarks across 5 ATM scale profiles demonstrated sub-second audit latencies (1.01\,s for a 444-component national grid), confirming negligible CI/CD overhead. Furthermore, we documented an industrial gap analysis and established a comprehensive experimental protocol to evaluate defect detection effectiveness, detector precision scaling, and GNN criticality model accuracy during ongoing SaaG-D deployment trials.

\textbf{Future Work:} We plan to complete SaaG-D implementation, execute the planned empirical evaluation trials in live staging pipelines, implement delta-aware gating against merge bases, and integrate autonomous LLM-driven coding agents to generate remediation pull requests when gating violations occur.

\begin{thebibliography}{18}
\bibitem{icao2005}
International Civil Aviation Organization (ICAO). 2005. \emph{Global Air Traffic Management Operational Concept}. Doc 9854, AN/458.

\bibitem{eurocontrol2020}
EUROCONTROL. 2020. \emph{EUROCONTROL Specification for ASTERIX Data Category Definitions}, Edition 2.4.

\bibitem{omg2015dds}
Object Management Group (OMG). 2015. \emph{Data Distribution Service (DDS) Specification}, Version 1.4.

\bibitem{bass2021}
L. Bass, P. Clements, and R. Kazman. 2021. \emph{Software Architecture in Practice} (4th ed.). Addison-Wesley Professional.

\bibitem{feiler2012aadl}
P. H. Feiler and D. P. Gluch. 2012. \emph{Model-Based Engineering with AADL: An Introduction to the SAE Architecture Analysis \& Design Language Standard}. Addison-Wesley.

\bibitem{humble2010}
J. Humble and D. Farley. 2010. \emph{Continuous Delivery: Reliable Software Releases through Build, Test, and Deployment Automation}. Addison-Wesley.

\bibitem{eugster2003}
P. Th. Eugster, P. A. Felber, R. Guerraoui, and A.-M. Kermarrec. 2003. The many faces of publish/subscribe. \emph{ACM Computing Surveys} 35, 2 (2003), 114--131.

\bibitem{carzaniga2001}
A. Carzaniga, D. S. Rosenblum, and A. L. Wolf. 2001. Design and evaluation of a wide-area event notification service. \emph{ACM Transactions on Computer Systems} 19, 3 (2001), 332--383.

\bibitem{brandes2001}
U. Brandes. 2001. A faster algorithm for betweenness centrality. \emph{Journal of Mathematical Sociology} 25, 2 (2001), 163--177.

\bibitem{tao2019digital}
F. Tao, H. Zhang, A. Liu, and A. Y. C. Nee. 2019. Digital Twin in Industry: State-of-the-Art. \emph{IEEE Transactions on Industrial Informatics} 15, 4 (2019), 2405--2415.

\bibitem{tarjan1972}
R. E. Tarjan. 1972. Depth-first search and linear graph algorithms. \emph{SIAM Journal on Computing} 1, 2 (1972), 146--160.

\bibitem{saaty1980}
T. Saaty. 1980. \emph{The Analytic Hierarchy Process}. McGraw-Hill, New York.

\bibitem{vlissides1994}
J. Vlissides, R. Helm, R. Johnson, and E. Gamma. 1994. \emph{Design Patterns: Elements of Reusable Object-Oriented Software}. Addison-Wesley.

\bibitem{fowler2002}
M. Fowler. 2002. \emph{Patterns of Enterprise Application Architecture}. Addison-Wesley.

\bibitem{lee2008cps}
E. A. Lee. 2008. Cyber Physical Systems: Design Challenges. In \emph{IEEE ISORC '08}. 363--369.

\bibitem{avizienis2004}
A. Avizienis, J.-C. Laprie, B. Randell, and C. Landwehr. 2004. Basic concepts and taxonomy of dependable and secure computing. \emph{IEEE TDSC} 1, 1 (2004), 11--33.

\bibitem{shaw1996}
M. Shaw and D. Garlan. 1996. \emph{Software Architecture: Perspectives on an Emerging Discipline}. Prentice Hall.

\bibitem{kiczales1997}
G. Kiczales, J. Lamping, A. Mendhekar, C. Maeda, C. Lopes, J.-M. Loingtier, and J. Irwin. 1997. Aspect-oriented programming. In \emph{ECOOP '97}. Springer, 220--242.
\end{thebibliography}

\end{document}
